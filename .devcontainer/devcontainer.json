{
  "name": "Eli-Mini",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "22"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y --no-install-recommends chromium ffmpeg && python3.12 -m venv .venv && .venv/bin/python -m pip install -U pip && .venv/bin/python -m pip install -U -r requirements.txt",
  "postStartCommand": ".venv/bin/python -m pip install -U -r requirements.txt",
  "remoteEnv": {
    "PATH": "${containerWorkspaceFolder}/.venv/bin:${containerEnv:PATH}",
    "YOUTUBE_BROWSER_PATH": "/usr/bin/chromium"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python"
      }
    }
  }
}
